* text=auto

# This is to satisfy shellcheck(SC1017).
*.sh text eol=lf

# Enforce Windows newlines for C# files to avoid false positives with IDE0055 warning.
# See https://github.com/Lombiq/Open-Source-Orchard-Core-Extensions/issues/106
*.cs text eol=crlf
