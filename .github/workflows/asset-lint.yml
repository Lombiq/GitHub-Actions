name: Lint Scripts, Styles, and Texts

on:
  workflow_call:
    secrets:
      CHECKOUT_TOKEN:
        required: false
        description: >
          The GitHub token to authenticate checkout with. Pass in a GitHub personal access token if authenticated
          submodules are used.

    inputs:
      cancel-workflow-on-failure:
        description: When set to "true", it will cancel the current workflow run with all jobs if this workflow fails.
        type: string
        default: 'true'
      scripts:
        description: |
          Information about the projects where JavaScript linting should occur. It's a comma separated list of the
          project paths. Optionally, each list item can be in the "{project path}:{glob pattern}" format as well, if
          you don't want to target all files in the project directory.
        type: string
        default: .:.
      styles:
        description: Information about the projects where CSS linting should occur. Has the same format as the "scripts" input above.
        type: string
        default: .:**/*.css
      lint-markdown:
        description: Indicates if Markdown files should be linted in this repository.
        type: string
        default: 'true'
      markdown-config:
        description: Path to a file to use for the base configuration object (defaults to "lombiq.markdownlint.json").
        type: string
        default: lombiq.markdownlint.json
      markdown-globs:
        description: Glob expression(s) of files to lint (newline-delimited).
        type: string
        default: >
          **/*.{md,markdown}
          !node_modules
          !License.md

jobs:
  lint:
    name: Lint Scripts, Styles, and Texts
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: Lombiq/GitHub-Actions/.github/actions/checkout@dev
        with:
          token: ${{ secrets.CHECKOUT_TOKEN }}

      - name: Set up Node.js
        uses: Lombiq/GitHub-Actions/.github/actions/setup-node@issue/OSOE-1185

      - name: Lint Scripts and Styles
        uses: Lombiq/GitHub-Actions/.github/actions/asset-lint@issue/OSOE-1185
        with:
          scripts: ${{ inputs.scripts }}
          styles: ${{ inputs.styles }}

      - name: Markdown Linting
        if: inputs.lint-markdown == 'true'
        uses: Lombiq/GitHub-Actions/.github/actions/markdown-lint@issue/OSOE-1185
        with:
          config: ${{ inputs.markdown-config }}
          globs: ${{ inputs.markdown-globs }}

      - name: Cancel Workflow on Failure
        if: failure() && inputs.cancel-workflow-on-failure == 'true'
        uses: Lombiq/GitHub-Actions/.github/actions/cancel-workflow@dev
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
